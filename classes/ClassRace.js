export class Race {
  constructor(id, description, name, image, isActive) {
    this.id = id,
    this.description = description,
    this.name = name,
    this.image = image,
    this.isActive = isActive
  }
}